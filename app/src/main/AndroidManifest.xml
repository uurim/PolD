<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jobcho.pold">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        tools:replace="android:label"
        android:icon="@mipmap/ic_pold"
        android:label="Pol:D"
        android:roundIcon="@mipmap/ic_pold_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PolD">
        <activity
            android:name="com.jobcho.pold.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.jobcho.pold.MainActivity" />
    </application>

</manifest>